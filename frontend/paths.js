/**
 * este archivo nos sirve como acortador de las rutas de la aplicacion por las que podemos navegar.
 * en este caso, solo ofrecemos la ruta acortada de "public" para luego acceder a los archivos html
 * de manera mas facil
 */
module.exports = {
    pages:`${__dirname}/public`
}